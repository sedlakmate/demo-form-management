{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "public/dist/**"],
      "dependsOn": ["^build"],
      "env": ["NEXT_PUBLIC_API_HOST", "BACKEND_URL"]
    },
    "test": {
      "outputs": ["coverage/**"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "dependsOn": ["^build"],
      "env": ["BACKEND_URL"]
    },
    "type-check": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["BACKEND_URL"]
    },
    "clean": {
      "cache": false
    },
    "check": {
      "dependsOn": ["lint", "type-check", "test"],
      "outputs": [],
      "cache": false,
      "persistent": false,
      "inputs": ["$TURBO_DEFAULT$"],
      "env": ["BACKEND_URL", "NEXT_PUBLIC_API_HOST"]
    }
  }
}
